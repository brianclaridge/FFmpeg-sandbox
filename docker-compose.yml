services:
  audio-processor:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ${CLAUDE_PROJECT_SLUG:-.}/.data:/app/.data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
